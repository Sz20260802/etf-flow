# 部署指引（小白版，照着点就行）

目标：把 ETF 资金流系统部署到 Streamlit Cloud，获得一个随时随地能打开的网址。
全程免费，预计 20 分钟。

---

## 第一步：注册 GitHub（如已完成可跳过）

1. 打开 https://github.com → 点右上角 **Sign up**
2. 输入邮箱 → 设密码 → 输入用户名 → 按提示完成邮箱验证
3. 记下你的**用户名**（后面要用）

## 第二步：安装 GitHub Desktop（傻瓜式上传工具）

1. 打开 https://desktop.github.com → 下载并安装
2. 打开 GitHub Desktop → **Sign in to GitHub.com** → 用刚注册的账号登录

## 第三步：创建仓库并上传代码

1. GitHub Desktop 左上角 **File → New Repository**
   - Name：`etf-flow`（就用这个名字，和指引一致）
   - Local Path：选一个你找得到的文件夹（如 文档\GitHub）
   - ✅ 勾选 Initialize this repository with a README
   - 点 **Create Repository**
2. 点工具栏 **Repository → Show in Explorer**（Mac 是 Reveal in Finder），打开仓库文件夹
3. 把**部署包里所有文件和文件夹**复制进去（app.py、pages、db、collectors、data、docs、.streamlit、config.py 等，连同 data 文件夹里的两个 .db 数据库文件一起）
   - 如果提示替换 README.md，选替换
4. 回到 GitHub Desktop，左侧会列出全部文件 → 左下角输入框随便写 `init` → 点 **Commit to main**
5. 点顶部 **Publish repository** → 取消勾选 "Keep this code private"（想私有也行，Streamlit 一样能部署）→ 点 **Publish**
   - 两个数据库文件较大（约 180MB），上传需要几分钟，耐心等进度条走完

## 第四步：部署到 Streamlit Cloud

1. 打开 https://share.streamlit.io → 点 **Sign in with GitHub** → 授权
2. 点右上角 **Create app**
3. 选择 **Deploy a public app from GitHub**：
   - Repository：选 `Sz20260802/etf-flow`
   - Branch：`main`
   - Main file path：`app.py`
4. 点 **Deploy** → 等 3-5 分钟（首次要安装依赖）
5. 看到页面打开就成功了！网址形如 `https://你的应用名.streamlit.app`，收藏它

## 日常使用须知（重要）

- **数据自动更新**：每次有人打开应用、检测到新交易日，系统会在后台自动采集当天数据，无需你做任何操作
- **资金流指标从第 2 个交易日起出数**：免费源没有历史日份额，系统部署后逐日积累，这是所有免费方案的物理边界（β压强/资金流类页面首日为空属正常）
- **实例重启**：免费版应用闲置会休眠，再次打开时自动唤醒并补更；极端情况下实例重置，数据会回到仓库里的种子库状态，之后照常积累
- **穿透/β压强为估算口径**：估算股数，不等同于现货成交或真实持仓变更

## 常见问题

| 现象 | 原因与处理 |
|---|---|
| Deploy 后一直转圈 | 首次安装依赖慢，等 5 分钟；超过 10 分钟看右下角 Manage app → Logs |
| 页面显示"数据库未初始化" | data 文件夹没传上去，回第三步检查两个 .db 文件 |
| 上传 .db 失败/卡住 | 网络问题，GitHub Desktop 里 Push origin 重试；或把应用改为首启自动回填（代码已内置兜底，只是首日图表会少历史K线） |
| 想看更新日志 | share.streamlit.io → 你的应用 → Manage app → Logs |

---

遇到问题截图发我，我来排查。
